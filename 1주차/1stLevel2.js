//2. json array
const teamMember = [
    {
        "name": "한수아"
        "part": "Server"
        "status" : "YB"
    }
];


const getMemberList = teamMember.filter((m) => m.name === "한수아")
(ers) = {
    members.filter(member => {
        console 
    })
}